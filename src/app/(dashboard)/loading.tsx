import { Stack, Typography } from "@mui/material";

export default function Loading() {
  return (
    <Stack>
      <Typography>This is loading before page show.....</Typography>
    </Stack>
  );
}
